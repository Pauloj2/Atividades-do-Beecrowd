#include <stdio.h>

int main(){

    float A, B, soma, media;

    scanf ("%f", &A);
    scanf ("%f", &B);

    soma = (A * 3.5) + (B * 7.5);
    media = soma / 11;

    printf("MEDIA = %.5f\n", media);


return 0;
}
