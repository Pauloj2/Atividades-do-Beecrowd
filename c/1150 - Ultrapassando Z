#include <stdio.h>

int main() {

    int x, z, i, soma = 0, num = 0;

    scanf("%d", &x);
    while(z <= x){
        scanf("%d", &z);
    }

    for(i=x; i<z; i++){
        soma+= i;
        num++;
        if(soma > z){
            break;
        }
    }
    printf("%d\n", num);

    return 0;
}
