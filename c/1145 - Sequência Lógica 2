#include <stdio.h>

int main() {

    int x, y, i, cont = 0;

    scanf("%d %d", &x, &y);

    for(i=0+1; i<=y; i++){
        printf("%d", i);
        if(i % x == 0){
            printf("\n");
        }else {
            printf(" ");
        }
    }
    return 0;
}
