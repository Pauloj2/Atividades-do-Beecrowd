#include <stdio.h>

int main (){

    int vet[10];
    int i;

    vet[0] = 5;

    for(i=1; i<10; i++){
        vet[i] = vet[i-1] + 5;
    }
    for (i=0; i<10; i++){
        printf("Vetor[%d] = %d\n", i, vet[i]);
    }

    return 0;
}
