#include <stdio.h>

int main () {

    int num, i, cal = 0;

    scanf("%d", &num);
    if (num % 2 != 0){
        num = num +1;
    }
    for(i=1; i<=num; i+=2){
        cal = (i+1)* (i+1);
        printf("%d^2 = %d\n", i+1, cal);
    }

    return 0;
}
