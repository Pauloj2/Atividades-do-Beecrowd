#include <stdio.h>

int main () {

    int i, cont = 0;
    double num;

    for(i=1; i<=6; i++){
        scanf("%lf", &num);
        if(num > 0){
            cont= cont + 1;
        }
    }
    printf("%d valores positivos\n", cont);
    return 0;
}
