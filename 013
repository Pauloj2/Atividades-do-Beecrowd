#include <stdio.h>

int main (){

    int vet[10];
    int i;

    for(i=0; i<10; i++){
        if(i % 2 == 0 || i == 0){
            vet[i] = 5;
        }else{
            vet[i] = 3;
        }
    }
    for (i=0; i<10; i++){
        printf("Vetor[%d] = %d\n", i, vet[i]);
    }

    return 0;
}
